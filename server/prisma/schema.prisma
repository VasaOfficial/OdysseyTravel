// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

// run this to migrate to db docker exec -it backend npx prisma migrate dev --name init

// to enter database from terminal docker exec -it postgres_db psql -U postgres

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Continent {
  id        Int            @id @default(autoincrement())
  name      String
  countries Country[]
}

model Country {
  id          Int           @id @default(autoincrement())
  name        String
  continent   Continent     @relation(fields: [continentId], references: [id])
  continentId Int
  destinations Destination[]
}

model Destination {
  id           Int          @id @default(autoincrement())
  countryName  String  
  country      Country      @relation(fields: [countryId], references: [id])
  countryId    Int
  city         String       @db.VarChar(100)
  price        Int
  latitude     Float
  longitude    Float
  imageUrl     String  @db.VarChar(255)
  durationDays Int
  description  String  @db.Text
  tripRoute    String  @db.Text
  travelPlan  TravelPlan[]
  includedIn   String        @db.Text
}

model TravelPlan {
  id             Int          @id @default(autoincrement())
  destination    Destination  @relation(fields: [destinationId], references: [id])
  destinationId  Int
  day            String
  description    String       @db.Text
}